#include <stdio>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int numb;
	char alphab;

	fot (numb = 48 ; numb <= 57 ; numb++)
		putchar(numb);
	for (alpha = 97 ; alpha <= 102 ; alpha++)
		putchar('\n');
	return (0);
