#include "main,h"

/**
*_isdigit - function that prints the sign of a number
*
*Return: 1 if c is a digit, o otherwise
*/

int _isdigit(c)
{
if (c >= '0' && c <= '9')
{
return(1);
}
else
return (0);
}
